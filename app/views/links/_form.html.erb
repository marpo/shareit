<%= form_for(@link, :html => { :class => "well form-inline"}) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
    <%= f.label :titel %><br />
    <%= f.text_field :titel, :class => 'input-small' %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url, :class => 'input-small' %>
  </div>
  <div class="field">
    <%= f.label :beschreibung %><br />
    <%= f.text_area :beschreibung, rows: 6, :class => 'imput-xlarge' %>
  </div>
  
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
